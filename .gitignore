# Secrets & local config
.env
config/upstreams.local.yaml
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

# THSDK 登录会话文件（thsdk 包写入进程工作目录，内含账号会话，禁止提交）
account.session
*.session

# Logs & runtime data
logs/*.log
logs/*.db
logs/*.db-shm
logs/*.db-wal
logs/cache_stats.json
data/

# Session / planning artifacts (not part of the product)
.remember/
.remember/**
.playwright-mcp/
.playwright-mcp/**
docs/superpowers/

# One-off debug / scratch
%TEMP%*
*.tmp
*.bak

# Old handoff (kept locally, archived elsewhere)
.handoff/